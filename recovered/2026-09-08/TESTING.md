# Verificación L0 · 8 de septiembre de 2026

Los metadatos suplementarios recuperan propietarios, ACL, privilegios por defecto, límites de secuencias como texto y ajustes Edge. `workflow-results.json` conserva pruebas con usuarios y datos ficticios en PostgreSQL local aislado. Los fallos registrados son defectos del código recuperado, no correcciones desplegadas.

El laboratorio conjunto, con scripts y archivos para repetir las pruebas, se entrega en `outputs/L0-Laboratorio.zip` del espacio de trabajo original de esta tarea. No está publicado en GitHub. La referencia mantiene el código original del repositorio separado del desplegado; no aplicar los archivos `*-reference` como migraciones.

Consulte los informes `L0-Validacion-y-siguiente-lote.md` y `L0-Conciliacion-Edge.md` de la entrega. La certificación de navegador, almacenamiento real, proveedores e integración de publicación queda para sus lotes específicos.
